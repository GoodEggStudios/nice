meta {
  name: List Buttons
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/buttons
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  ~limit: 50
  ~cursor: 
}

docs {
  # List all buttons
  
  Returns all buttons for your site with pagination.
  
  ## Query Parameters
  
  - `limit` - Max results (default: 50, max: 100)
  - `cursor` - Pagination cursor from previous response
}
