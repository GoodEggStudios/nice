meta {
  name: Update Button
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/buttons/{{privateId}}
  body: json
  auth: none
}

body:json {
  {
    "restriction": "global",
    "label": "Nice",
    "pressed_label": "Nice'd"
  }
}

docs {
  # Update Button
  
  Update button settings using the private ID.
  
  ## Path Parameters
  
  | Parameter | Description |
  |-----------|-------------|
  | privateId | Private ID (ns_xxx format) |
  
  ## Request Body
  
  All fields are optional. Only provided fields will be updated.
  
  | Field | Type | Values |
  |-------|------|--------|
  | restriction | string | `url`, `domain`, `global` |
  | theme | string | `light`, `dark`, `minimal`, `mono-dark`, `mono-light` |
  | size | string | `xs`, `sm`, `md`, `lg`, `xl` |
  | multi_nice | boolean | Enable/disable clap-style multi-nice |
  | label | string | Visible idle label; maximum 32 Unicode code points; leading/trailing whitespace is trimmed; ASCII control characters are rejected |
  | pressed_label | string | Visible pressed label; maximum 32 Unicode code points; leading/trailing whitespace is trimmed; ASCII control characters are rejected |
  
  ## Response
  
  ```json
  {
    "id": "n_xxx",
    "url": "https://example.com",
    "restriction": "global",
    "count": 42,
    "theme": "light",
    "size": "md",
    "created_at": "...",
    "embed": { ... }
  }
  ```
}
