meta {
  name: Get Button
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/buttons/{{buttonId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Get button details
  
  Returns details for a specific button including embed code.
}
