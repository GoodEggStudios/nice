meta {
  name: Delete Button
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/buttons/{{privateId}}
  body: none
  auth: none
}

docs {
  # Delete Button
  
  Delete a button using the private ID. **This cannot be undone.**
  
  ## Path Parameters
  
  | Parameter | Description |
  |-----------|-------------|
  | privateId | Private ID (ns_xxx format) |
  
  ## Response
  
  ```json
  {
    "success": true,
    "message": "Button deleted"
  }
  ```
}
