meta {
  name: Verify Site
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/sites/{{siteId}}/verify
  body: none
  auth: none
}

docs {
  # Verify domain ownership
  
  After adding the DNS TXT record from registration, call this endpoint to verify.
  
  Once verified, you can create buttons for your site.
}
